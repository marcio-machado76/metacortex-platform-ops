"""Camada de interpretação — grupo 4 em diante, fora do escopo deste commit.

Transforma o envelope cru de `cliente.py` no retrato exibível do namespace.
Não importa `kubernetes` nem `urllib3`: só recebe dicionários e devolve
dicionários e valores simples.
"""
